{
  "name": "mcp",
  "version": "1.0.0",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "copy-md": "copyfiles --error --up 1 \"src/**/*.md\" build/src",
    "clean": "rimraf build",
    "prebuild": "npm run clean",
    "postbuild": "npm run copy-md",
    "start": "node build/index.js",
    "dev": "tsx watch src/index.ts"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "copyfiles": "^2.4.1",
    "rimraf": "^6.1.3",
    "tsx": "^4.23.12",
    "typescript": "^7.0.2"
  }
}
